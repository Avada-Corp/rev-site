# 📋 Инструкция для бекенда: Реализация функционала заморозки аккаунтов и статуса кредитора

## 🎯 Обзор требований

Frontend ожидает следующие данные и эндпоинты для работы с заморозкой аккаунтов пользователей и управлением статусом кредитора. Заморозка применяется на уровне пользователя (не API ключа), что означает, что при заморозке пользователя блокируются все его API ключи. Статус кредитора также применяется на уровне пользователя и определяет, может ли он работать в кредит.

## 📊 Новые поля данных

### 1. Поля в модели User
```javascript
// Добавить в модель User
freezePeriod: {
  type: Number,
  default: null, // null означает отсутствие заморозки
  min: 1,
  max: 365 // дни
},
canWorkOnCredit: {
  type: Boolean,
  default: false // по умолчанию пользователь не может работать в кредит
}
```

### 2. Системная настройка по умолчанию
```javascript
// В коллекции settings или отдельной коллекции defaultSettings
{
  key: 'defaultFreezePeriod',
  value: null, // количество дней по умолчанию
  type: 'number'
}
```

## 🚀 Новые эндпоинты API

### 1. Обновление срока заморозки пользователя
```
POST /admin/updateUserFreezePeriod
```

**Request Body:**
```json
{
  "email": "user@example.com",
  "freezePeriod": 30
}
```

**Response:**
```json
{
  "status": true,
  "message": "Срок заморозки обновлен",
  "data": {
    "email": "user@example.com",
    "freezePeriod": 30
  }
}
```

**Логика:**
- Найти пользователя по email
- Установить поле freezePeriod
- Сохранить изменения
- Вернуть обновленные данные

### 2. Снятие заморозки пользователя
```
POST /admin/removeUserFreezePeriod
```

**Request Body:**
```json
{
  "email": "user@example.com"
}
```

**Response:**
```json
{
  "status": true,
  "message": "Заморозка снята",
  "data": {
    "email": "user@example.com",
    "freezePeriod": null
  }
}
```

**Логика:**
- Найти пользователя по email
- Установить freezePeriod = null
- Сохранить изменения

### 3. Получение настройки по умолчанию
```
GET /admin/getDefaultFreezePeriod
```

**Response:**
```json
{
  "status": true,
  "data": 30 // или null если не установлено
}
```

**Логика:**
- Получить значение defaultFreezePeriod из настроек системы

### 4. Обновление настройки по умолчанию
```
POST /admin/updateDefaultFreezePeriod
```

**Request Body:**
```json
{
  "freezePeriod": 30 // или null для снятия
}
```

**Response:**
```json
{
  "status": true,
  "message": "Настройка по умолчанию обновлена",
  "data": 30
}
```

**Логика:**
- Обновить значение defaultFreezePeriod в настройках системы

### 5. Обновление статуса кредитора пользователя
```
POST /admin/updateUserCreditorStatus
```

**Request Body:**
```json
{
  "email": "user@example.com",
  "canWorkOnCredit": true
}
```

**Response:**
```json
{
  "status": true,
  "message": "Статус кредитора обновлен",
  "data": {
    "email": "user@example.com",
    "canWorkOnCredit": true
  }
}
```

**Логика:**
- Найти пользователя по email
- Установить поле canWorkOnCredit
- Сохранить изменения
- Вернуть обновленные данные

## 🔄 Модификация существующих эндпоинтов

### 1. Получение пользователей (GET /admin/users)
**Текущий Response:**
```json
{
  "status": true,
  "data": [
    {
      "email": "user@example.com",
      "username": "username",
      "parentRef": "ref123",
      "userRole": "User",
      "regDate": "2024-01-01T00:00:00.000Z",
      // ... другие поля
    }
  ]
}
```

**Обновленный Response:**
```json
{
  "status": true,
  "data": [
    {
      "email": "user@example.com",
      "username": "username",
      "parentRef": "ref123",
      "userRole": "User",
      "regDate": "2024-01-01T00:00:00.000Z",
      "freezePeriod": 30, // ДОБАВИТЬ ЭТО ПОЛЕ
      "canWorkOnCredit": false, // ДОБАВИТЬ ЭТО ПОЛЕ
      // ... другие поля
    }
  ]
}
```

**Изменения:**
- Добавить поле `freezePeriod` в выборку данных пользователей
- Добавить поле `canWorkOnCredit` в выборку данных пользователей
- Значение `freezePeriod` может быть `null` (нет заморозки) или числом (количество дней)
- Значение `canWorkOnCredit` - булево значение (true/false)

## 🛠️ Технические детали реализации

### 1. Валидация данных
```javascript
// Валидация freezePeriod
const freezePeriodSchema = {
  type: Number,
  min: 1,
  max: 365,
  default: null
};
```

### 2. Middleware для проверки прав
```javascript
// Только для супер-админов
const requireSuperAdmin = (req, res, next) => {
  if (req.user.role !== 'SuperAdmin') {
    return res.status(403).json({
      status: false,
      message: 'Доступ запрещен'
    });
  }
  next();
};
```

### 3. Обработка ошибок
```javascript
// Стандартный формат ошибок
{
  status: false,
  message: "Пользователь не найден",
  errors: ["User with email 'user@example.com' not found"]
}
```

## 🔐 Аутентификация и авторизация

Все новые эндпоинты должны:
1. Требовать Bearer токен в заголовке Authorization
2. Проверять, что пользователь - супер-админ
3. Логировать действия для аудита

## 📈 Мониторинг и логирование

Рекомендуется логировать:
- Изменения срока заморозки пользователя
- Изменения настройки по умолчанию
- Попытки несанкционированного доступа

## 🧪 Тестирование

Необходимо протестировать:
1. Установку заморозки для существующего пользователя
2. Снятие заморозки
3. Установку/изменение настройки по умолчанию
4. Валидацию данных (отрицательные числа, слишком большие значения)
5. Права доступа (только супер-админы)
6. Установку статуса кредитора (true/false) для пользователя
7. Изменение статуса кредитора с true на false и наоборот
8. Проверка, что статус кредитора корректно сохраняется и возвращается в API

## 📝 Пример реализации на Node.js/Express

```javascript
// routes/admin.js
router.post('/updateUserFreezePeriod', requireSuperAdmin, async (req, res) => {
  try {
    const { email, freezePeriod } = req.body;

    // Валидация
    if (freezePeriod !== null && (freezePeriod < 1 || freezePeriod > 365)) {
      return res.status(400).json({
        status: false,
        message: 'Некорректный срок заморозки'
      });
    }

    // Обновление пользователя
    const user = await User.findOneAndUpdate(
      { email },
      { freezePeriod },
      { new: true }
    );

    if (!user) {
      return res.status(404).json({
        status: false,
        message: 'Пользователь не найден'
      });
    }

    res.json({
      status: true,
      message: 'Срок заморозки обновлен',
      data: {
        email: user.email,
        freezePeriod: user.freezePeriod
      }
    });
  } catch (error) {
    console.error('Error updating freeze period:', error);
    res.status(500).json({
      status: false,
      message: 'Внутренняя ошибка сервера'
    });
  }
});

router.post('/updateUserCreditorStatus', requireSuperAdmin, async (req, res) => {
  try {
    const { email, canWorkOnCredit } = req.body;

    // Валидация
    if (typeof canWorkOnCredit !== 'boolean') {
      return res.status(400).json({
        status: false,
        message: 'Некорректное значение статуса кредитора'
      });
    }

    // Обновление пользователя
    const user = await User.findOneAndUpdate(
      { email },
      { canWorkOnCredit },
      { new: true }
    );

    if (!user) {
      return res.status(404).json({
        status: false,
        message: 'Пользователь не найден'
      });
    }

    res.json({
      status: true,
      message: 'Статус кредитора обновлен',
      data: {
        email: user.email,
        canWorkOnCredit: user.canWorkOnCredit
      }
    });
  } catch (error) {
    console.error('Error updating creditor status:', error);
    res.status(500).json({
      status: false,
      message: 'Внутренняя ошибка сервера'
    });
  }
});
```

Эта инструкция охватывает все необходимые изменения для полной интеграции функционала заморозки аккаунтов и управления статусом кредитора между frontend и backend.
